# Ruby CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-ruby/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/ruby:2.4-node-browsers
      # - image: circleci/postgres:9.6-alpine

    working_directory: ~/picup

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v2-dependencies-{{ checksum "Gemfile.lock" }}
          - v2-dependencies-

      - run:
          name: install dependencies
          command: bundle install --jobs=4 --retry=3 --path vendor/bundle

      - save_cache:
          paths:
            - ./vendor/bundle
          key: v2-dependencies-{{ checksum "Gemfile.lock" }}
        
      # Database setup
      - run: bundle exec rails db:create
      - run: bundle exec rails db:migrate
      - run: bundle exec rails db:schema:load

      # run tests
      - run: bundle exec rails test

      # collect reports
      - store_test_results:
          path: /test-results
      - store_artifacts:
          path: /artifacts
notify:
  command: |
    curl -X POST -d '{"content": "hogehoge"}' -H 'Content-Type: application/json' https://discordapp.com/api/webhooks/407192311890444288/Ww7jt9ZDM-1K0VgBZ4kNo0bmkAJT9mYDSJSB0xTYF6FGVvIYJbRphCJs1hZfFfnfUFNr
